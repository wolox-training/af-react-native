import { StyleSheet } from 'react-native';
import { TURQUOISE, DUSTY_GRAY } from '@constants/colors';
const styles = StyleSheet.create({
  focused: {
    color: TURQUOISE
  },
  inactive: {
    color: DUSTY_GRAY
  }
});

export default styles;
